contents:
  repositories:
  - https://dl-cdn.alpinelinux.org/alpine/v3.15/main
  packages:
  - musl
  - zlib

accounts:
  groups:
  - groupname: nonroot
    gid: 10000
  users:
  - username: nonroot
    uid: 10000
  run-as: nonroot

os-release:
  version-id: '3.15'

#  docker run --rm -v $pwd/:/app:rw -w /app ghcr.io/chainguard-dev/apko:v0.3.3 build rootless.yaml alpine:3.15-apko apko.tar
#  docker load -i .\apko.tar
